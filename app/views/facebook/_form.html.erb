<%= error_messages_for(@admin_user) %>

<table summary="Admin user form fields">
  <tr>

  <tr>
    <th><%= f.label(:"Your email") %></th>
    <td>    <div class="tab-pane fade active in" id="form"></td>
  </tr>
  <tr>
    <th><%= f.label(:"Your Crush Name") %></th>
    <td><%= f.text_field(:username) %></td>
  </tr>
  <tr>
    <th><%= f.label(:"Hint to Your Friends") %></th>
    <td><%= f.text_field(:username) %></td>
  </tr>
</table>
